spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/third?useUnicode=true&characterEncoding=utf8
    username: root
    password: root12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8080
  context-path: /third